---
summary: "Breaking: search_investigations total→totalCount, lookup_vehicles output reshape + full pagination; mcp-ts-core ^0.6.12, landing page"
breaking: true
---

# 0.7.0 — 2026-04-23

## Changed

- **Breaking:** `nhtsa_search_investigations` output field `total` renamed to `totalCount` for naming parity
- **Breaking:** `nhtsa_lookup_vehicles` output reshaped — `count` and `totalAvailable` dropped in favor of unified `totalCount` + `returned` + `offset` + `limit` across every operation
- **Breaking:** `nhtsa_lookup_vehicles` `limit`/`offset` now apply to all operations via client-side slicing
- `nhtsa_decode_vin` description clarified for batch form
- Bumped `@cyanheads/mcp-ts-core` ^0.6.12; `@biomejs/biome` ^2.4.13; `vitest` ^4.1.5
- `createApp()` now configures `landing.tagline` + `landing.repoRoot` for per-tool view-source links
- Documented `MCP_PUBLIC_URL` env var in `.env.example`

## Added

- `skills/api-linter` and `skills/release-and-publish` — new skills from the framework
- Refreshed 13 skills from the framework (add-app-tool, add-prompt, add-resource, add-service, add-tool, api-context, api-services, api-utils, design-mcp-server, field-test, maintenance, polish-docs-meta, setup, report-issue-framework, report-issue-local)
