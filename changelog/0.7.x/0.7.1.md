---
summary: "mcp-ts-core ^0.7.0; recursive describe-on-fields linter adoption; security-pass skill; changelog scripts"
breaking: false
---

# 0.7.1 — 2026-04-24

## Added

- `skills/security-pass` v1.1 — MCP-flavored security audit skill
- `scripts/build-changelog.ts`, `scripts/check-docs-sync.ts`, `scripts/check-skills-sync.ts` — framework scripts synced; devcheck now enforces `CLAUDE.md`/`AGENTS.md` byte-parity and skills sync
- `.github/ISSUE_TEMPLATE/bug_report.yml` and `feature_request.yml` synced to framework template

## Changed

- Bumped `@cyanheads/mcp-ts-core` ^0.7.0 — flattened ZodError messages, structured `issues` on `McpError.data`, locale-aware `format-parity` numeric matching, directory-based changelog convention, recursive `describe-on-fields` linter
- Added `.describe()` on array-element objects and `z.union()` variants across all 7 tool definitions
- `CLAUDE.md`/`AGENTS.md` now reference `security-pass` in "What's Next?" and skills table
- Framework skill refreshes: `api-linter` v1.1, `maintenance` v1.5, `release-and-publish` v2.1, `report-issue-framework` v1.3, `report-issue-local` v1.3
