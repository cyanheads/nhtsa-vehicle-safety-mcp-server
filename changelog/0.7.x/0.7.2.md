---
summary: "mcp-ts-core ^0.8.19; typed errors[] + ctx.fail on 4 tools; httpErrorFromResponse; pluralize helper; Node >=24"
breaking: false
---

# 0.7.2 — 2026-05-08

## Added

- `src/services/nhtsa/format.ts` — shared `formatStars`, `formatRolloverProbability`, and `pluralize` helpers
- `scripts/check-framework-antipatterns.ts` and `scripts/split-changelog.ts` — synced from framework
- `skills/api-canvas`, `skills/api-telemetry`, `skills/tool-defs-analysis` — new skills from framework

## Changed

- Bumped `@cyanheads/mcp-ts-core` ^0.7.0 → ^0.8.19 — typed error contracts, `httpErrorFromResponse`, framework-antipattern linter, refreshed skill set
- Bumped engines: Node >=22.0.0 → >=24.0.0; `@biomejs/biome` ^2.4.14; `@types/node` ^25.6.2; `tsc-alias` ^1.8.17
- Pinned Docker base image to `oven/bun:1.3`
- Converted `nhtsa_decode_vin`, `nhtsa_get_safety_ratings`, `nhtsa_lookup_vehicles`, `nhtsa_search_recalls` to typed `errors[]` contracts with `ctx.fail(reason, ...)`
- `NhtsaService` now wraps upstream HTTP failures with `httpErrorFromResponse`
- `nhtsa_get_safety_ratings` field descriptions flag variant scope and cross-reference sibling tools
- Complaint and recall renderers now pluralize counts via shared `pluralize` helper
- Dropped unused `dev:stdio`/`dev:http` scripts from `package.json`
- Framework skill refresh: `add-service`, `add-tool`, `api-config`, `api-context`, `api-errors`, `api-linter`, `api-utils`, `api-workers`, `design-mcp-server`, `field-test`, `maintenance`, `release-and-publish`, `report-issue-framework`, `report-issue-local`, `security-pass`, `setup`
